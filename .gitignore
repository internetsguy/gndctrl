# Python build / venv scratch
.venv/
venv/
*.egg-info/
__pycache__/
*.pyc
*.pyo
build/
dist/

# Env
.env
.env.*
!.env.example

# gndctrl runtime state (machine-managed — never commit)
.gndctrl.locks

# Staged inbound drops (provenance only, integrated by hand)
incoming-*/
